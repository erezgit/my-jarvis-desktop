# WebContentsView Test

This is a **test MDX file** to verify that our WebContentsView MDX preview system is working correctly.

## Features
- MDX rendering with React components
- Hot Module Replacement (HMR)  
- Three-panel layout (file tree, preview, terminal)
- Secure IPC communication

## Test Components

<div style={{background: '#dbeafe', padding: '16px', borderRadius: '8px'}}>
  This is a custom React component in MDX!
</div>

## Code Example

```javascript
function HelloWorld() {
  return <h1>Hello from MDX Preview!</h1>
}
```

**Status**: Testing WebContentsView implementation from ticket 10.